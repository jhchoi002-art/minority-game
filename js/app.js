
var APP_VERSION="12.0.0";
const firebaseConfig={apiKey:"AIzaSyB5oFSIDXhzHaFgTR5cr1LvGHXFNStLSWk",authDomain:"minority-game-45d67.firebaseapp.com",databaseURL:"https://minority-game-45d67-default-rtdb.asia-southeast1.firebasedatabase.app",projectId:"minority-game-45d67",storageBucket:"minority-game-45d67.firebasestorage.app",messagingSenderId:"49514258718",appId:"1:49514258718:web:b89014318539af2e13e90f"};
firebase.initializeApp(firebaseConfig); const db=firebase.database();
const App={el:null,mode:"home",room:"",state:{},ref:null,renderKey:""};
const rawBank=Array.isArray(window.QUESTION_BANK)?window.QUESTION_BANK:[];
const qbank=rawBank.map((x,i)=>({id:x.id||i+1,category:x.category||"기본",q:x.q||x.question||`문제 ${i+1}`,o:(x.o||x.options||["보기 1","보기 2","보기 3"]).slice(0,3)}));
const $=id=>document.getElementById(id); const esc=v=>String(v??"").replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m])); const now=()=>Date.now(); const roomRef=(room=App.room)=>db.ref('rooms/'+room);
function makeCode(){const c='ABCDEFGHJKLMNPQRSTUVWXYZ23456789';return Array.from({length:6},()=>c[Math.floor(Math.random()*c.length)]).join('')}
function sid(){let s=localStorage.getItem('mg12_sid'); if(!s){try{s='s_'+crypto.randomUUID()}catch(e){s='s_'+Math.random().toString(36).slice(2)+Date.now()} localStorage.setItem('mg12_sid',s)} return s}
function url(mode,room=App.room){return `${location.pathname}?mode=${mode}${room?'&room='+encodeURIComponent(room):''}`} function go(m,r=App.room){location.href=url(m,r)} function fullUrl(m){return location.origin+url(m)} function studentUrl(){return fullUrl('student')} function displayUrl(){return fullUrl('display')} function qrUrl(t){return 'https://api.qrserver.com/v1/create-qr-code/?size=260x260&data='+encodeURIComponent(t)}
function copyText(t){navigator.clipboard.writeText(t).then(()=>alert('복사했습니다.'))} function openDisplay(){const w=window.open(displayUrl(),'_blank'); if(!w){copyText(displayUrl()); alert('팝업 차단으로 전자칠판 주소를 복사했습니다.')}}
function settings(st=App.state){return Object.assign({gameMode:'personal',teamCount:4,teamAssign:'choice'},st.settings||{})} function isTeam(){return settings().gameMode==='team'} function teamCount(){return Math.max(2,Math.min(10,Number(settings().teamCount||4)))} function teamAssign(){return settings().teamAssign||'choice'}
function countsOf(st=App.state){const opts=st.options||[], vals=Object.values(st.answers||{}); return {counts:opts.map((_,i)=>vals.filter(a=>Number(a.choice)===i).length), total:vals.length}}
function scoresFor(counts){const u=[...new Set(counts)].sort((a,b)=>a-b); if(u.length===1)return counts.map(()=>50); if(u.length===2)return counts.map(c=>c===u[0]?50:30); return counts.map(c=>c===u[0]?50:c===u[1]?40:30)}
function ranks(st=App.state){return Object.entries(st.scores||{}).map(([id,s])=>({id,name:s.name||'이름없음',team:s.team||'',total:Number(s.total||0),last:Number(s.last||0)})).sort((a,b)=>b.total-a.total)}
function teamStats(st=App.state){const parts=Object.values(st.participants||{}), ss=Object.values(st.scores||{}), arr=[]; for(let i=1;i<=teamCount();i++){const members=parts.filter(p=>String(p.team)===String(i)).length, scored=ss.filter(s=>String(s.team)===String(i)), total=scored.reduce((a,b)=>a+Number(b.total||0),0); arr.push({team:String(i),name:i+'조',members,total,avg:scored.length?Math.round(total/scored.length):0})} return arr.sort((a,b)=>b.avg-a.avg)}
function pickBalancedTeam(st=App.state){const parts=Object.values(st.participants||{});let min=999,c=[];for(let i=1;i<=teamCount();i++){const n=parts.filter(p=>String(p.team)===String(i)).length;if(n<min){min=n;c=[String(i)]}else if(n===min)c.push(String(i))}return c[Math.floor(Math.random()*c.length)]||'1'}
function myAnswer(){const a=(App.state.answers||{})[sid()]; return a&&a.choice!==undefined?Number(a.choice):-1}
function showFatal(err){console.error(err); App.el=$('app')||App.el; if(App.el)App.el.innerHTML=`<main class="home"><section class="hero"><h1>화면 표시 오류</h1><p>오류가 발생했습니다.</p><pre class="errorBox">${esc(err&&err.message?err.message:err)}</pre><button onclick="go('home','')">처음 화면으로</button></section></main>`} function safe(fn){try{fn()}catch(e){showFatal(e)}}
function listen(render,keyFn=null){if(!App.room)return home(); if(App.ref)App.ref.off(); App.ref=roomRef(); App.ref.on('value',snap=>{App.state=snap.val()||{}; if(keyFn){const k=keyFn(App.state); if(k===App.renderKey)return; App.renderKey=k} safe(render)},err=>showFatal(err))}
function init(){try{App.el=$('app'); const p=new URLSearchParams(location.search); App.mode=p.get('mode')||'home'; App.room=(p.get('room')||'').toUpperCase(); if(App.mode==='teacher')teacher(); else if(App.mode==='student')student(); else if(App.mode==='display')display(); else home()}catch(e){showFatal(e)}}
function home(){App.el.innerHTML=`<main class="home"><section class="hero"><div class="version">Ultimate v${APP_VERSION}</div><h1>🎮 소수결 게임</h1><p>Stable 재작성 · 개인전/팀전 · 학생 접속 현황 · 전자칠판 실시간 그래프</p><div class="homeGrid"><div class="card"><h2>방 만들기</h2><label>방 이름</label><input id="roomTitle" value="소수결 게임"><label>교사 이름</label><input id="teacherName" placeholder="예: 최정훈"><button id="createRoomBtn" onclick="createRoom()">새 방 만들기</button></div><div class="card"><h2>방 참가하기</h2><label>방코드</label><input id="joinCode" placeholder="예: A8KJX2" oninput="this.value=this.value.toUpperCase()"><button class="blue" onclick="joinRoom('student')">학생으로 참가</button><button class="ghost" onclick="joinRoom('teacher')">교사용으로 열기</button><button class="ghost" onclick="joinRoom('display')">전자칠판으로 열기</button></div></div></section></main>`}
async function createRoom(){const btn=$('createRoomBtn'), code=makeCode(); if(btn){btn.disabled=true;btn.textContent='방 만드는 중...'} try{await roomRef(code).set({title:$('roomTitle')?.value||'소수결 게임',teacher:$('teacherName')?.value||'선생님',appVersion:APP_VERSION,createdAt:now(),settings:{gameMode:'personal',teamCount:4,teamAssign:'choice',scoreMode:'average'},round:0,question:'문제를 열어 주세요.',options:['치킨','피자','떡볶이'],openedAt:0,showResults:false,scored:false,pointMultiplier:1,participants:{},answers:{},scores:{},history:[]});go('teacher',code)}catch(e){alert('방 만들기에 실패했습니다.\nFirebase 규칙을 확인해 주세요.\n\n'+(e.message||e)); if(btn){btn.disabled=false;btn.textContent='새 방 만들기'}}}
function joinRoom(m){const c=($('joinCode').value||'').trim().toUpperCase(); if(!c)return alert('방코드를 입력해 주세요.'); go(m,c)}
function teacher(){listen(renderTeacher)}
function questionDraft(){const f=qbank[0]||{q:'하루 동안 한 가지 맛만 먹는다면?',o:['달콤한 맛','짭짤한 맛','매운맛']};let opts=f.o;try{opts=JSON.parse(localStorage.getItem('mg12_opts_'+App.room)||'null')||f.o}catch(e){}return{q:localStorage.getItem('mg12_q_'+App.room)||f.q,o:[opts[0]||'보기 1',opts[1]||'보기 2',opts[2]||'보기 3']}}
function saveDraft(){localStorage.setItem('mg12_q_'+App.room,$('question')?.value||'');localStorage.setItem('mg12_opts_'+App.room,JSON.stringify([0,1,2].map(i=>$('opt'+i)?.value||`보기 ${i+1}`)))}
function renderTeacher(){const s=settings(),d=questionDraft(),expected=Number(localStorage.getItem('mg12_expected_'+App.room)||25),mult=Number(localStorage.getItem('mg12_mult_'+App.room)||1),ct=countsOf(),parts=Object.keys(App.state.participants||{}).length,progress=Math.min(100,Math.round(ct.total/Math.max(1,expected)*100)); App.el.innerHTML=`<main class="dash"><header class="dashHeader"><div><h1>소수결 게임 <span>Ultimate v${APP_VERSION}</span></h1><p>방코드 <b class="code">${App.room}</b> · ${esc(App.state.title||'')}</p></div><button class="ghost" onclick="go('home','')">처음으로</button></header><section class="summary"><div><span>모드</span><b>${s.gameMode==='team'?'팀전':'개인전'}</b></div><div><span>제출</span><b>${ct.total}/${expected}</b></div><div><span>접속</span><b>${parts}명</b></div><div><span>라운드</span><b>${App.state.round||0}</b></div></section><section class="card"><div class="stageHead"><h2>학생 접속 현황</h2><div class="pill">제출 ${ct.total}명 / 접속 ${parts}명</div></div>${participantStatusHtml()}</section><section class="card"><h2>수업 설정</h2><div class="settingGrid"><div><label>게임 모드</label><select id="gameMode" onchange="saveSettings()"><option value="personal" ${s.gameMode==='personal'?'selected':''}>개인전</option><option value="team" ${s.gameMode==='team'?'selected':''}>팀전</option></select></div><div class="${s.gameMode==='team'?'':'off'}"><label>조 개수</label><input id="teamCount" type="number" min="2" max="10" value="${s.teamCount}" ${s.gameMode==='team'?'':'disabled'} onchange="saveSettings()"></div><div><label>이번 문제 배수</label><input type="number" min="1" max="10" value="${mult}" onchange="localStorage.setItem('mg12_mult_${App.room}',Math.max(1,Number(this.value)||1));renderTeacher()"></div><div><label>예상 참여 인원</label><input type="number" value="${expected}" onchange="localStorage.setItem('mg12_expected_${App.room}',this.value);renderTeacher()"></div></div>${s.gameMode==='team'?`<div class="teamPanel"><h3>팀 설정</h3><div class="settingGrid two"><div><label>조 배정 방식</label><select id="teamAssign" onchange="saveSettings()"><option value="choice" ${s.teamAssign==='choice'?'selected':''}>학생이 직접 선택</option><option value="random" ${s.teamAssign==='random'?'selected':''}>랜덤 균형 배정</option></select></div><div><label>팀 점수</label><input value="팀 평균 점수" disabled></div></div>${teamSummaryHtml()}</div>`:''}</section><section class="grid2"><div class="card"><h2>학생 접속</h2><div class="codeBox">${App.room}</div><div class="urlbox">${esc(studentUrl())}</div><button class="smallBtn" onclick="copyText('${esc(studentUrl())}')">학생 주소 복사</button><button class="smallBtn" onclick="openDisplay()">전자칠판 열기</button><button class="smallBtn" onclick="copyText('${esc(displayUrl())}')">전자칠판 주소 복사</button><br><img class="qr" src="${qrUrl(studentUrl())}"></div><div class="card"><h2>문제은행</h2><div class="bankControls"><select id="cat"><option>전체</option>${[...new Set(qbank.map(x=>x.category))].map(c=>`<option>${esc(c)}</option>`).join('')}</select><input id="search" placeholder="검색"></div><button onclick="randomQuestion()">랜덤 문제</button><button class="ghost" onclick="listQuestions()">목록 보기</button><div id="qList" class="qList"></div></div></section><section class="card"><h2>문제 준비</h2><label>문제</label><input id="question" value="${esc(d.q)}" oninput="saveDraft()"><div class="optionGrid">${[0,1,2].map(i=>`<div><label>보기 ${i+1}</label><input id="opt${i}" value="${esc(d.o[i])}" oninput="saveDraft()"></div>`).join('')}</div><button onclick="openQuestion()">문제 열기 / 다음 문제</button><button class="secondary" onclick="resetAnswers()">응답 초기화</button></section><section class="card stage"><div class="stageHead"><h2>${App.state.round?App.state.round+'번. ':''}${esc(App.state.question||'문제를 열어 주세요.')} ${Number(App.state.pointMultiplier||1)>1?'🔥 '+App.state.pointMultiplier+'배':''}</h2><div class="pill">제출 ${ct.total}명 / 예상 ${expected}명</div></div><div class="progress"><div style="width:${progress}%"></div></div><button class="reveal" onclick="showResults()">라운드 종료 / 결과 공개 + 점수 반영</button>${App.state.showResults?resultBars(ct.counts,scoresFor(ct.counts),Number(App.state.pointMultiplier||1)):`<p class="hint bigHint">라운드 진행 중입니다. 학생들은 결과 공개 전까지 선택을 바꿀 수 있습니다.</p>`}</section><section class="rankGrid">${isTeam()?`<div class="card"><h2>팀 랭킹</h2>${teamRankHtml()}</div>`:''}<div class="card"><div class="stageHead"><h2>개인 랭킹</h2><div><button class="ghost" onclick="downloadCSV()">CSV 저장</button><button class="ghost" onclick="resetScores()">점수 초기화</button></div></div>${rankHtml(ranks())}</div></section>${isTeam()?`<section class="card"><h2>참가자 조 관리</h2>${participantManagerHtml()}</section>`:''}<section class="card"><h2>문제별 통계</h2>${historyHtml(App.state.history||[])}</section></main>`}
function participantStatusHtml(){const p=App.state.participants||{},a=App.state.answers||{},rows=Object.entries(p).sort((x,y)=>Number(y[1].at||0)-Number(x[1].at||0)); if(!rows.length)return `<p class="hint">아직 접속한 학생이 없습니다. 학생이 이름을 입력하면 1라운드 시작 전에도 표시됩니다.</p>`; const t=now(); return `<div class="participantStatusList">${rows.map(([id,v])=>{const ans=a[id]&&a[id].choice!==undefined,last=Number(v.at||0),recent=last&&(t-last<300000),min=last?Math.floor((t-last)/60000):null,team=v.team?`${v.team}조`:(isTeam()?'조 미선택':'개인전');return `<div class="participantStatusItem ${ans?'answered':'waiting'}"><div><b>${esc(v.name||'이름없음')}</b><small>${team}</small></div><span class="statusBadge ${ans?'ok':'wait'}">${ans?'제출':'접속'}</span><em>${recent?'접속 중':(min===null?'시간 없음':`${min}분 전`)}</em></div>`}).join('')}</div>`}
async function saveSettings(){const gm=$('gameMode').value,tc=Math.max(2,Math.min(10,Number($('teamCount')?.value)||4)),ta=$('teamAssign')?.value||teamAssign();await roomRef().child('settings').update({gameMode:gm,teamCount:tc,teamAssign:ta,scoreMode:'average'})}
function filteredQuestions(){const c=$('cat')?.value||'전체',k=($('search')?.value||'').trim();return qbank.filter(x=>(c==='전체'||x.category===c)&&(!k||x.q.includes(k)||x.o.join(' ').includes(k)))} function listQuestions(){$('qList').innerHTML=filteredQuestions().slice(0,100).map(x=>`<button class="qItem" onclick="loadQuestion(${x.id})"><b>${esc(x.category)}</b> ${esc(x.q)}</button>`).join('')} function loadQuestion(id){const x=qbank.find(q=>q.id===id);if(!x)return;$('question').value=x.q;[0,1,2].forEach(i=>$('opt'+i).value=x.o[i]||`보기 ${i+1}`);saveDraft()} function randomQuestion(){const l=filteredQuestions();loadQuestion((l[Math.floor(Math.random()*l.length)]||qbank[0]).id)}
async function openQuestion(){const q=($('question').value||'문제').trim(),opts=[0,1,2].map(i=>$('opt'+i).value.trim()||`보기 ${i+1}`);localStorage.setItem('mg12_q_'+App.room,q);localStorage.setItem('mg12_opts_'+App.room,JSON.stringify(opts));await roomRef().update({question:q,options:opts,openedAt:now(),showResults:false,scored:false,round:Number(App.state.round||0)+1,pointMultiplier:Number(localStorage.getItem('mg12_mult_'+App.room)||1),answers:{}})} async function resetAnswers(){await roomRef().update({answers:{},showResults:false,scored:false})} async function resetScores(){if(confirm('점수를 초기화할까요?'))await roomRef().update({scores:{}})} async function showResults(){const snap=await roomRef().once('value'),st=snap.val()||{},ct=countsOf(st),sc=scoresFor(ct.counts),ans=st.answers||{},old=st.scores||{},next={...old},mult=Number(st.pointMultiplier||1);if(!st.scored){Object.entries(ans).forEach(([id,a])=>{const add=(sc[Number(a.choice)]||0)*mult;next[id]={name:a.name||old[id]?.name||'이름없음',team:a.team||old[id]?.team||'',total:Number(old[id]?.total||0)+add,last:add,lastChoice:Number(a.choice)}})}const history=[...(Array.isArray(st.history)?st.history:[]),{round:st.round,question:st.question,options:st.options,counts:ct.counts,score:sc,pointMultiplier:mult,at:now()}].slice(-50);await roomRef().update({showResults:true,scored:true,scores:next,history})}
function resultBars(counts,score,mult){const max=Math.max(1,...counts);return `<div class="results">${(App.state.options||[]).map((o,i)=>`<div class="result"><div class="row"><b>${esc(o)}</b><span>${counts[i]}명 · ${score[i]*mult}점</span></div><div class="meter"><div style="width:${Math.round(counts[i]/max*100)}%"></div></div></div>`).join('')}</div>`} function rankHtml(list){return list.length?`<div class="scoreboard">${list.map((s,i)=>`<div class="scoreRow ${i===0?'topRank':''}"><span class="rank">${i+1}</span><b>${esc(s.name)} ${s.team?`<small>${s.team}조</small>`:''}</b><span>총 ${s.total}점</span><em>이번 +${s.last}</em></div>`).join('')}</div>`:`<p class="hint">아직 점수가 없습니다.</p>`} function teamSummaryHtml(){return `<div class="teamSummary">${teamStats().sort((a,b)=>Number(a.team)-Number(b.team)).map(t=>`<div><b>${t.name}</b><span>${t.members}명 참가</span><em>평균 ${t.avg}점</em></div>`).join('')}</div>`} function teamRankHtml(){return `<div class="teamRankBox">${teamStats().map((t,i)=>`<div class="teamRank ${i===0?'firstTeam':''}"><span>${i===0?'🥇':i===1?'🥈':i===2?'🥉':i+1}</span><b>${t.name}</b><em>평균 ${t.avg}점</em><small>${t.members}명</small></div>`).join('')}</div>`} function participantManagerHtml(){const rows=Object.entries(App.state.participants||{});return rows.length?`<div class="participantList">${rows.map(([id,p])=>`<div class="participantItem"><b>${esc(p.name||'이름없음')}</b><select onchange="moveTeam('${id}',this.value)"><option value="">미배정</option>${Array.from({length:teamCount()},(_,i)=>i+1).map(n=>`<option value="${n}" ${String(p.team)===String(n)?'selected':''}>${n}조</option>`).join('')}</select></div>`).join('')}</div>`:`<p class="hint">아직 참가자가 없습니다.</p>`} async function moveTeam(id,team){const up={};up[`participants/${id}/team`]=team;if((App.state.answers||{})[id])up[`answers/${id}/team`]=team;if((App.state.scores||{})[id])up[`scores/${id}/team`]=team;await roomRef().update(up)} function historyHtml(h){return h.length?`<div class="historyList">${h.slice(-8).reverse().map(x=>`<div class="historyItem"><b>${x.round}번. ${esc(x.question)}</b><div>${(x.options||[]).map((o,i)=>`${esc(o)} ${(x.counts||[])[i]||0}명`).join(' · ')}</div></div>`).join('')}</div>`:`<p class="hint">아직 통계가 없습니다.</p>`} function downloadCSV(){const rows=[['순위','이름','조','총점','이번점수']];ranks().forEach((s,i)=>rows.push([i+1,s.name,s.team?`${s.team}조`:'',s.total,s.last]));const csv=rows.map(r=>r.map(v=>`"${String(v).replaceAll('"','""')}"`).join(',')).join('\n');const a=document.createElement('a');a.href=URL.createObjectURL(new Blob(['\ufeff'+csv],{type:'text/csv;charset=utf-8'}));a.download=`소수결_${App.room}.csv`;a.click()}
let nameTimer=null,heartTimer=null,composing=false; function studentKey(st){const my=(st.answers||{})[sid()]||{},me=(st.participants||{})[sid()]||{};return [st.round||0,st.question||'',JSON.stringify(st.options||[]),st.openedAt||0,st.showResults?1:0,st.scored?1:0,JSON.stringify(st.settings||{}),my.choice??'',me.team||''].join('|')} function student(){startHeartbeat();listen(renderStudent,studentKey)} async function touchParticipant(){const input=$('sName'),name=(input?input.value:localStorage.getItem('mg12_name')||'').trim();if(!name)return;let team=localStorage.getItem('mg12_team_'+App.room)||'';const me=(App.state.participants||{})[sid()]||{};if(me.team)team=me.team;const sel=$('sTeam');if(isTeam()&&teamAssign()==='choice'&&sel&&sel.value)team=sel.value;if(isTeam()&&teamAssign()==='random'&&!team){team=pickBalancedTeam();localStorage.setItem('mg12_team_'+App.room,team)}await roomRef().child('participants/'+sid()).update({name,team:team||'',at:now(),online:true})} function scheduleTouch(){clearTimeout(nameTimer);nameTimer=setTimeout(()=>touchParticipant().catch(()=>{}),700)} function startHeartbeat(){clearInterval(heartTimer);heartTimer=setInterval(()=>touchParticipant().catch(()=>{}),15000)}
function renderStudent(){const name=localStorage.getItem('mg12_name')||'',me=(App.state.participants||{})[sid()]||{},localTeam=localStorage.getItem('mg12_team_'+App.room)||'',team=me.team||localTeam,ended=!!App.state.showResults||!!App.state.scored,opened=Number(App.state.openedAt||0),sel=myAnswer(),can=opened&&!ended&&(!isTeam()||teamAssign()==='random'||team);App.el.innerHTML=`<main class="student"><section class="studentCard"><h1>소수결 게임</h1><p class="center">방코드 <b class="code">${App.room}</b> · ${isTeam()?'팀전':'개인전'}</p><div class="studentInfo"><label>이름 또는 번호</label><input id="sName" value="${esc(name)}" oncompositionstart="composing=true" oncompositionend="composing=false;localStorage.setItem('mg12_name',this.value);scheduleTouch()" oninput="localStorage.setItem('mg12_name',this.value);if(!composing)scheduleTouch()" onblur="composing=false;touchParticipant()">${isTeam()&&teamAssign()==='choice'?`<label>조 선택</label><select id="sTeam" onchange="localStorage.setItem('mg12_team_${App.room}',this.value);touchParticipant().then(()=>renderStudent())"><option value="">조를 선택하세요</option>${Array.from({length:teamCount()},(_,i)=>i+1).map(n=>`<option value="${n}" ${String(team)===String(n)?'selected':''}>${n}조</option>`).join('')}</select>`:''}${isTeam()&&teamAssign()==='random'?`<div class="assigned">${team?`🎉 ${team}조에 배정되었습니다.`:'이름을 입력하면 자동으로 조가 배정됩니다.'}</div>`:''}</div><h2>${App.state.round?App.state.round+'번. ':''}${esc(App.state.question||'선생님이 문제를 열 때까지 기다려 주세요.')}</h2><div class="choices">${(App.state.options||[]).map((o,i)=>`<button class="choice ${sel===i?'selectedChoice':''}" ${!can?'disabled':''} onclick="submitAnswer(${i})">${esc(o)}${sel===i?'<small>선택됨</small>':''}</button>`).join('')}</div><p class="status">${!name.trim()?'이름을 입력하면 선생님 화면에 접속 현황이 표시됩니다.':ended?'라운드가 종료되어 선택을 바꿀 수 없습니다.':!opened?'대기 중입니다. 선생님이 문제를 열면 선택할 수 있습니다.':isTeam()&&teamAssign()==='choice'&&!team?'팀전입니다. 조를 선택하세요.':sel>=0?'선택되었습니다. 결과 공개 전까지 바꿀 수 있습니다.':'하나를 골라 주세요.'}</p></section></main>`}
async function submitAnswer(choice){const name=($('sName')?.value||'').trim();if(!name){alert('이름 또는 번호를 입력해 주세요.');$('sName')?.focus();return}await touchParticipant();const me=(App.state.participants||{})[sid()]||{};let team=me.team||localStorage.getItem('mg12_team_'+App.room)||'';if(isTeam()&&teamAssign()==='choice'&&!team){alert('조를 선택해 주세요.');return}if(App.state.showResults||App.state.scored){alert('라운드가 종료되어 선택할 수 없습니다.');return}await roomRef().child('answers/'+sid()).set({choice,name,team,at:now()})}
function display(){listen(renderDisplay)} function renderDisplay(){const ct=countsOf(),sc=scoresFor(ct.counts),sum=Math.max(1,ct.counts.reduce((a,b)=>a+b,0)),top=ranks().slice(0,8);App.el.innerHTML=`<main class="display"><section class="displayHeader"><div><div class="badge">${App.state.round?App.state.round+'번 문제':'대기 중'} · ${isTeam()?`팀전(${teamAssign()==='random'?'랜덤배정':'직접선택'})`:'개인전'} ${Number(App.state.pointMultiplier||1)>1?'🔥 '+App.state.pointMultiplier+'배':''}</div><h1>${esc(App.state.question||'선생님이 문제를 열 때까지 기다려 주세요.')}</h1></div><div class="submitBox"><b>${ct.total}</b><span>명 제출</span></div></section>${!App.state.showResults?`<section class="waiting"><div class="pulse">소수결을 노려보세요!</div><div class="displayChoices">${(App.state.options||[]).map((o,i)=>`<div>${i+1}. ${esc(o)}</div>`).join('')}</div>${isTeam()?`<div class="displayCard"><h2>팀 현황</h2>${teamSummaryHtml()}</div>`:''}</section>`:`<section class="displayGrid"><div class="displayCard"><h2>바로 전 문제 선택 비율</h2>${(App.state.options||[]).map((o,i)=>{const p=Math.round(ct.counts[i]/sum*100);return `<div class="barRow"><div class="barLabel"><b>${esc(o)}</b><span>${ct.counts[i]}명 · ${p}% · ${sc[i]*Number(App.state.pointMultiplier||1)}점</span></div><div class="bar"><div style="width:${Math.max(4,p)}%"></div></div></div>`}).join('')}</div><div class="displayCard"><h2>${isTeam()?'팀 랭킹':'개인 랭킹'} TOP</h2>${isTeam()?teamRankHtml():top.map((s,i)=>`<div class="rankBig"><span>${i===0?'🥇':i===1?'🥈':i===2?'🥉':i+1}</span><b>${esc(s.name)}</b><em>${s.total}점</em><small>+${s.last}</small></div>`).join('')}</div></section>`}</main>`}
init();
